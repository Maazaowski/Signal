# Web framework
fastapi==0.141.1
# Pinned explicitly: fastapi only requires >=0.46, and pip will happily leave a
# vulnerable transitive version in place on upgrade.
starlette==1.3.1
uvicorn==0.32.1

# HTTP + parsing
httpx==0.28.1
beautifulsoup4==4.12.3

# Data + config
pydantic==2.10.4
PyYAML==6.0.2
python-dotenv==1.2.3

# Templating
jinja2==3.1.6

# Scheduling
apscheduler==3.10.4

# Secrets at rest
cryptography==50.0.0

# Optional: Google Sheets export
gspread==6.2.1
google-auth==2.49.2
